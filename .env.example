# ====================================================================
# MODÈLE DE VARIABLES D'ENVIRONNEMENT POUR commercial-erp-senegal
#
# Instructions :
# 1. Copiez ce fichier et renommez la copie en ".env".
# 2. Placez le fichier .env respectif dans les dossiers /server et /client.
# 3. Remplissez les valeurs réelles.
# 4. NE JAMAIS COMMITTER LE FICHIER .env DANS GIT !
# ====================================================================


### === VARIABLES SERVEUR (Pour /server/.env) === ###

# -- Configuration Générale du Serveur --
# Environnement de l'application (development, production, test)
NODE_ENV=development

# Port sur lequel le serveur Express écoutera
PORT=5000

# URL du client (React), nécessaire pour la configuration CORS
CLIENT_URL=http://localhost:3000


# -- Connexion à la Base de Données (MongoDB) --
# Chaîne de connexion complète à votre base de données MongoDB
# Exemple local : mongodb://127.0.0.1:27017/commercial_erp_db
# Exemple Atlas : mongodb+srv://<user>:<password>@cluster-url/commercial_erp_db
MONGO_URI=mongodb+srv://libassedia:2412Laye@cluster0.rt5rwye.mongodb.net/erp_senegal


# -- Authentification (JSON Web Token) --
# Clé secrète TRES longue et complexe pour signer les tokens JWT
JWT_SECRET=REMPLACER_PAR_UN_SECRET_FORT_ET_ALEATOIRE_DE_PLUS_DE_32_CARACTERES

# Durée de validité du token (ex: 1d, 7d, 1h, 30m)
JWT_EXPIRES_IN=1d

# Durée de validité du cookie contenant le token (en jours)
JWT_COOKIE_EXPIRES_IN=1


# -- Configuration Email (pour les notifications, factures, etc.) --
# Utilisez un service comme SendGrid, Mailgun, ou votre propre serveur SMTP
EMAIL_HOST=smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_USER=votre_utilisateur_smtp
EMAIL_PASS=votre_mot_de_passe_smtp
EMAIL_FROM="Votre ERP <no-reply@votre-erp.sn>"


# -- APIs Tiers & Services (Spécifique Sénégal) --
# Clés API pour les intégrations de paiement Mobile Money (exemples)
ORANGE_MONEY_API_KEY=votre_cle_api_orange_money
WAVE_API_KEY=votre_cle_api_wave

# -- Stockage Cloud (Optionnel, pour les sauvegardes et uploads) --
# Type de stockage : 'local' ou 's3'
STORAGE_TYPE=local

# Si STORAGE_TYPE=s3, remplissez les clés AWS
AWS_ACCESS_KEY_ID=votre_cle_acces_aws
AWS_SECRET_ACCESS_KEY=votre_cle_secrete_aws
AWS_S3_BUCKET_NAME=nom_de_votre_bucket_s3
AWS_REGION=votre_region_aws


### === VARIABLES CLIENT (Pour /client/.env) === ###
# Important : les variables React doivent commencer par REACT_APP_

# URL de base de l'API backend pour que le client sache où envoyer les requêtes
REACT_APP_API_URL=http://localhost:5000/api

# Nom de l'application, peut être utilisé dans le titre ou les en-têtes
REACT_APP_APP_NAME="ERP Gestion Commerciale"