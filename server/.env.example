# ===============================================================
#  FICHIER D'EXEMPLE DE VARIABLES D'ENVIRONNEMENT
#  Copiez ce fichier en .env et remplissez les valeurs.
#  NE PAS COMMITTER LE FICHIER .env DANS GIT.
# ===============================================================

# --- CONFIGURATION DU SERVEUR ---
NODE_ENV=development # 'development' ou 'production'
PORT=5000

# --- CONFIGURATION DE LA BASE DE DONNÉES (MongoDB) ---
MONGO_URI=mongodb+srv://libassedia:2412Laye@cluster0.rt5rwye.mongodb.net/erp_senegal

# --- CONFIGURATION CLIENT (CORS) ---
# URL de votre application frontend (React). Essentiel pour la sécurité CORS en production.
CLIENT_URL=http://localhost:3000

# --- CONFIGURATION DE L'AUTHENTIFICATION (JWT) ---
# Utilisez un générateur de chaîne aléatoire forte pour le secret JWT
JWT_SECRET=votre_secret_jwt_super_long_et_complexe
JWT_EXPIRES_IN=30d # Expiration du token (ex: 30d, 1h, 7d)
JWT_COOKIE_EXPIRES_IN=30 # Expiration du cookie en jours (nombre entier)

# --- CONFIGURATION EMAIL (pour les notifications, factures, etc.) ---
# Exemple avec un service SMTP comme Mailgun, SendGrid ou un serveur local
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=votre_utilisateur_smtp
EMAIL_PASSWORD=votre_mot_de_passe_smtp
EMAIL_FROM_NAME="ERP Sénégal"
EMAIL_FROM_ADDRESS=noreply@erpsenegal.com

# --- CONFIGURATION DU STOCKAGE (pour les uploads et backups) ---
# Options : 'local', 's3', 'cloudinary'
STORAGE_PROVIDER=local

# Si vous utilisez Amazon S3 (recommandé pour la production)
# AWS_ACCESS_KEY_ID=votre_cle_acces_aws
# AWS_SECRET_ACCESS_KEY=votre_cle_secrete_aws
# AWS_S3_BUCKET_NAME=votre_nom_de_bucket_s3
# AWS_REGION=votre_region_aws_ex:eu-west-3

# --- CONFIGURATION DU CACHE (Redis) ---
# Recommandé pour la production pour améliorer les performances
# REDIS_HOST=127.0.0.1
# REDIS_PORT=6379
# REDIS_PASSWORD=votre_mot_de_passe_redis

# --- CLÉS API POUR SERVICES TIERS ---
# Pour l'envoi de SMS (ex: Twilio)
# SMS_PROVIDER_API_KEY=
# SMS_PROVIDER_AUTH_TOKEN=
# SMS_SENDER_PHONE=

# Pour les intégrations Mobile Money (Orange Money, Wave)
# ORANGE_MONEY_API_KEY=
# WAVE_API_KEY=

# --- CONFIGURATION DE L'ENTREPRISE (par défaut) ---
# Ces valeurs peuvent être utilisées pour initialiser les paramètres
DEFAULT_CURRENCY=XOF # Franc CFA
DEFAULT_COUNTRY_CODE=SN # Sénégal
DEFAULT_TAX_RATE=18 # Taux de TVA par défaut (18%)