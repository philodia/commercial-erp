# ===============================================================
#  FICHIER GITIGNORE POUR LE SERVEUR NODE.JS
# ===============================================================

# --- Fichiers d'environnement ---
# Ne jamais versionner les fichiers .env contenant des secrets.
# Seul le fichier .env.example doit être dans le dépôt.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Dépendances ---
# Ce dossier est généré à l'aide de 'npm install' ou 'yarn install'.
node_modules/

# --- Logs ---
# Les fichiers de logs sont spécifiques à l'environnement d'exécution.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Répertoires de build et de production ---
# Ces dossiers contiennent du code transpilé ou compilé.
dist/
build/
.next/
out/

# --- Fichiers de cache ---
.npm
.yarn-cache
.pnpm-store

# --- Fichiers générés par le système d'exploitation ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# --- Fichiers générés par les éditeurs de code et IDE ---
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.sublime-project
*.sublime-workspace

# --- Rapports de tests ---
# Les rapports de couverture de code sont générés à chaque test.
coverage/
.nyc_output/

# --- Fichiers de données et uploads ---
# Les fichiers uploadés par les utilisateurs ne font pas partie du code source.
# Ce dossier doit être sauvegardé par d'autres moyens en production.
uploads/
exports/

# --- Fichiers temporaires ---
*.tmp
*.swp